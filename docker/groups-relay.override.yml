# Overlay for verse-pbc/groups_relay: uses the production Dockerfile.
# The bundled Dockerfile.dev fails at `cargo build --features console`
# (per a comment in the production Dockerfile the feature is disabled on
# purpose).
services:
  groups_relay:
    build:
      dockerfile: Dockerfile
