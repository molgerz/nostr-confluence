# Group relay (NIP-29). Defaults to the local relay from scripts/dev-relay-up.sh
VITE_RELAY_URL=ws://localhost:8080

# Relays to fetch profiles (kind 0) from, comma-separated.
# A NIP-29 relay does not accept kind 0 — every event needs an h tag there —
# so profiles come from the user's own relays.
# To try locally:  nak serve --port 10577
# In production:   wss://purplepag.es,wss://relay.damus.io
VITE_PROFILE_RELAYS=

# Blossom server for attachments (images, files). Nostr stores no files;
# the blob lives there under its sha256, the event only holds the URL.
# To try locally:  node scripts/dev-blossom.mjs
VITE_BLOSSOM_SERVER=
