# Throwaway keys for local test relays — never commit
scripts/.dev-keys

node_modules/
dist/
.env
.env.local
db/

# local Claude Code settings
.claude/settings.local.json

# macOS
.DS_Store

# third-party repos cloned locally (relay source)
.local/
