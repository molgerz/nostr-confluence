# Throwaway keys for local test relays — never commit
scripts/.dev-keys

node_modules/
dist/
.env
.env.local
db/

# local Claude Code settings
.claude/settings.local.json

# scratch space for throwaway renders and previews
.scratch/

# macOS
.DS_Store

# third-party repos cloned locally (relay source)
.local/
.gstack/
